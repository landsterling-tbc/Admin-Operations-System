# ملف تسليم الجلسة — نظام العمليات الإدارية (Admin Operations System)

**آخر تحديث:** 13 سبتمبر 2026
**الغرض من الملف:** ملخص كامل لحالة المشروع عشان أي جلسة/محادثة جديدة مع Claude تقدر تكمل من غير ما تفقد أي سياق. اقرأ الملف ده كامل قبل ما تبدأ أي تعديل جديد.

---

## 1. نظرة عامة على المشروع

نظام إداري ثابت (Static Web App) بدون أي أدوات بناء (no build tools) وبدون أي Framework — شغال بـ HTML/CSS/JS خام + Supabase كـ Backend.

**الملفات الأساسية (كلها في جذر المجلد `D:\Admin-Operations-System`):**
- `index.html` — كل صفحات وواجهات النظام (SPA واحدة، تنقل بين `<section class="app-page">` مختلفة)
- `style.css` — نظام تصميم كامل بـ CSS Variables (Design Tokens)
- `script.js` — كل منطق التطبيق (ملف ضخم، +10,000 سطر)
- `config.js` — إعدادات الاتصال بـ Supabase (URL + anon key)
- `database/schema.sql` — المصدر الكامل لبنية قاعدة البيانات (Tables + RLS + Views + Triggers + Functions)
- `database/*.sql` — ملفات Migration منفصلة (يتم تشغيلها يدويًا في Supabase SQL Editor)
- `README.md` — توثيق عام للوحدات وحالة المشروع

**القواعد الثابتة اللي لازم تتبعها دايمًا:**
- الواجهة كلها RTL دايمًا، وكل النصوص لازم تكون **فصحى (Modern Standard Arabic)**، ممنوع العامية في أي نص واجهة أو تعليق كود.
- مفيش build tools ولا npm ولا React — كل حاجة Vanilla JS + CDN scripts (ExcelJS, SheetJS, Chart.js, Supabase JS SDK).
- أي تعديل CSS على الـ Design Tokens لازم يتحط في 3 أماكن: `:root`, `[data-theme="dark"]`, و `@media (prefers-color-scheme: dark) { :root:not([data-theme="light"]) }`.
- RLS (Row-Level Security) في Supabase هو الضامن الحقيقي للصلاحيات — إخفاء عناصر الواجهة (`hidden`) مجرد UX بس، مش حماية فعلية.

---

## 2. الأدوار (Roles) في النظام

الـ enum: `public.user_role` في schema.sql — 4 أدوار:

| الدور (بالكود) | الاسم بالعربي | الصلاحيات تقريبًا |
|---|---|---|
| `super_admin` | مدير النظام | يشوف ويعدّل كل حاجة |
| `admin` | الأدمن | زي سوبر أدمن تقريبًا في الوحدات التشغيلية (سيارات/وقود/عهدة/مصروفات) |
| `manager` | المدير | Read-only غالبًا، ملخصات فقط |
| `it_support` | الدعم التقني | يشوف أصول الـ IT بس — **ممنوع يشوف العهدة/المصروفات نهائيًا** في أي مكان بالنظام (حتى الرئيسية) |

دالة مهمة في schema.sql: `public.is_admin_or_above()` = `super_admin` أو `admin`.

---

## 3. الوحدات الموجودة فعليًا في النظام

1. **السيارات (Vehicles)** — بيانات + وقود + تخصيص + استيراد Excel
2. **الوقود (Fuel)** — معاملات تعبئة، تقارير اتجاه (رسم بياني لتر + تكلفة منفصلين مع أرقام على الخط)
3. **العهدة النقدية (Petty Cash Funds)** — عهدة واحدة عامة (مش لكل موظف)، حالات: `active` / `exhausted` / `closed`
4. **المصروفات (Expenses)** — أهم وحدة اتعمل فيها شغل كبير النهاردة (تفاصيل كاملة تحت في قسم 5)
5. **أصول تقنية المعلومات (IT Assets)** — 5 فئات: لابتوبات، بريد إلكتروني، شرائح SIM، تابلت، كتالوج لابتوبات
6. **المستخدمون والحسابات (Users)** — إدارة حسابات عبر Edge Function (RPC + JWT مخصص)
7. **لوحة التحكم (Dashboard)** — "نظرة عامة" شاملة (بديل عن "آخر العمليات" و"ملخص ذكي" اللي اتشالوا)
8. **مركز التقارير (Reports Center)** — تبويبات لكل وحدة + تصدير CSV/Excel
9. **سجل العمليات (Audit Log)**

---

## 4. آخر شغل اتعمل (جلسة اليوم بالكامل) — بالترتيب الزمني

### أ) الرسوم البيانية في التقارير
- فصل رسم الوقود لرسمين منفصلين جمب بعض (لتر + تكلفة) بدل رسم واحد بمحورين.
- الأرقام بقت مكتوبة فوق نقاط الخط مباشرة (`inlineDataLabels` plugin في Chart.js).
- زر تحميل PNG على كل كارت رسم بياني في النظام (`injectChartDownloadBtn`).

### ب) نسخة احتياطية كاملة (Excel)
- بقت تسحب بيانات IT Assets كمان (5 شيتات إضافية) — دالة مشتركة `fetchAndAddItAssetsSheets()`.

### ج) لوحة التحكم (Dashboard)
- اتشال "آخر العمليات" و"ملخص ذكي".
- اتحط بدالهم قسم "نظرة عامة" شامل (`renderDashboardOverview`) بيغطي كل الوحدات.
- الدعم التقني (`it_support`) بيشوف بس كارت أصول IT — دالة منفصلة `loadDashboardStatsForItSupport()`.

### د) تقرير العهدة النقدية (MEEM-FIN-F003) — أهم فيتشر
النظام بقى يقدر يصدّر تقرير Excel مطابق تمامًا للتقرير الحقيقي اللي بيتبعت للشركة الأم (كود القالب: MEEM-FIN-F003)، مبني على بيانات فعلية من قاعدة البيانات مش ملف يدوي.

**حقول اتضافت لجدول `expenses`:**
- `supplier_name` (اسم المورد) — اختياري
- `invoice_number` (رقم الفاتورة) — اختياري
- `recipient_employee_name` (الموظف المستلم للمبلغ فعليًا) — **داخلي بالكامل، لمعرفة الأدمن فقط، لا يظهر أبدًا في تقرير MEEM ولا في أي جدول عام أو استعلام قوائم** (مستثنى عمدًا من `loadExpenses()`)

**دوال تصدير Excel المهمة في script.js:**
- `exportMeemPettyCashExcel()` — تقرير العهدة الرسمي (شيتين: ملخص الأصناف + تفاصيل MEEM-FIN-F003)، بمعادلات SUM/SUMIF/COUNTIF حية
- `exportExpenseRecipientsExcel()` — تقرير منفصل تمامًا لمن استلم المبالغ (أدمن/سوبر أدمن فقط)
- `exportItAssetsExcel()` — تصدير أصول IT فقط

**تحميل تلقائي يومي صامت (`runDailyAutoDownloads`)** — عند فتح الموقع أول مرة في اليوم:
- `super_admin` / `admin` → نسخة احتياطية كاملة + تقرير MEEM + تقرير المستلمين
- `it_support` → تقرير أصول IT فقط
- `manager` → لا شيء
- التتبع عبر `localStorage` بمفتاح `autoDownloadDate_<userId>` (مرة واحدة يوميًا لكل مستخدم)

### هـ) فئات المصروفات (Expense Categories)
جدول `expense_categories` اتوسّع لـ **30 فئة** (كانت فاضية قبل كده)، مبنية على تحليل تقرير MEEM الحقيقي + فئات شائعة إضافية. آخر فئة اتضافت: "أثاث ومعدات مكتبية (شاشات، طاولات، كراسي)".

**⚠️ الفئات دي لازم تتأكد إنها اتشغلت فعليًا في قاعدة البيانات الحية** عبر:
`database/migration-expense-supplier-invoice-and-categories.sql`

### و) حقل الفئة بقى Combobox قابل للبحث
مش `<select>` عادي ولا `datalist` — Combobox مخصّص (HTML: `.combo-field` + `.combo-dropdown` في CSS) بيعرض كل الفئات عند الفتح ويفلتر أثناء الكتابة، مع دعم كامل للوحة المفاتيح (أسهم + Enter + Escape).

### ز) فئة "أخرى" الذكية
لو المستخدم اختار فئة "أخرى" في فورم إضافة/تعديل مصروف، بيظهر حقل إجباري "اسم الفئة الجديدة" — وقت الحفظ بينشئ فئة حقيقية جديدة فعليًا في `expense_categories` بدل ما يفضل المصروف تحت "أخرى" بلا معنى.

**⚠️ محتاج تشغيل migration:** `database/migration-allow-admin-add-expense-category.sql`
(كان الإدراج في `expense_categories` مقصور على `super_admin` بس، اتعدّل لـ `is_admin_or_above()` عشان الأدمن العادي يقدر ينشئ فئة جديدة من "أخرى").

### ح) اختيار العهدة يدويًا عند تسجيل مصروف
**المشكلة اللي اتحلت:** كل مصروف كان بيترّبط تلقائيًا بالعهدة النشطة الحالية بس، فمصروف قديم (حد صرف من جيبه واستنى العهدة) كان بيتسجل غلط على عهدة جديدة.

**الحل:**
- فورم "إضافة مصروف" بقى فيه `<select>` "العهدة النقدية *" يعرض **كل** العهدات (نشطة/مستنفدة/مغلقة) مع رصيد كل واحدة، مختار افتراضيًا العهدة النشطة لكن قابل للتغيير يدويًا.
- لو اخترت عهدة غير نشطة يظهر تنبيه تحذيري واضح.
- في استيراد Excel: عمود اختياري جديد "كود العهدة (اختياري)" — لو فاضي بيتحدد تلقائيًا على النشطة، ولو محدد بيتخصم من العهدة دي بالذات. فحص الرصيد بقى **لكل عهدة على حدة** (مش رصيد واحد إجمالي).
- مفيش أي تعديل RLS/Schema هنا — كانت الصلاحية موجودة أصلًا.

### ط) إمكانية تعديل المصروفات (مش بس إلغاء)
زر "تعديل" جديد جنب "إلغاء" على كل مصروف نشط. بيفتح نفس فورم الإضافة معبّى بالبيانات الحالية (شامل الفئة والعهدة والمستلم)، وبيعمل `UPDATE` بدل `INSERT`. فحص الرصيد ذكي: بيرجّع المبلغ القديم للحساب لو العهدة نفسها متغيرتش، وبيتأكد من رصيد العهدة الجديدة لو اتغيرت.

المصروفات الملغاة (Voided) **مفيهاش** زر تعديل عمدًا — سجل نهائي.

### ي) إصلاح CSS مهم: سكرول الـ Modals
اكتشفنا إن كل الفورمات في النظام (14 فورم) عندها نفس المشكلة: الـ `<form>` نفسه هو الابن المباشر لـ `.modal` (مش `.modal-body`)، فمكنش بياخد ارتفاع محدود، فكان بيتقطع بدل ما يعمل سكرول — وده كان بيخفي زرار الحفظ لما الحقول تكتر (زي ما حصل في فورم المصروفات بعد إضافة كل الحقول الجديدة).

**الحل (في `style.css`):**
```css
.modal > form {
  display: flex;
  flex-direction: column;
  flex: 1 1 auto;
  min-height: 0;
  overflow: hidden;
}
.modal-body {
  padding: var(--space-5);
  overflow-y: auto;
  flex: 1 1 auto;
  min-height: 0;
}
```
الإصلاح ده عام لكل الفورمات في النظام مش بس فورم المصروفات.

---

## 5. ملفات Migration اللي محتاجة تتشغل في Supabase SQL Editor

**مهم جدًا:** لازم تسأل المستخدم دايمًا هل شغّل الملفات دي ولا لأ قبل ما تفترض إن أي فيتشر شغالة فعليًا. آخر مرة اتأكد فيها (قبل الجلسة دي تخلص) إن أول 2 ملفين اشتغلوا، لكن لازم تتأكد تاني:

1. ✅ `database/migration-expense-supplier-invoice-and-categories.sql` — تم تشغيله وتأكيده (30 فئة موجودة، الأعمدة الجديدة موجودة)
2. ⚠️ `database/migration-allow-admin-add-expense-category.sql` — **آخر حالة معروفة: لسه ما اتأكدش إنه اتشغل** (كان لسه قديم وقت آخر فحص)

**فيه ملف فحص جاهز:** `database/check-todays-migrations-status.sql` — استعلام واحد بيرجع جدول ✅/❌ لكل بند. شغّله في SQL Editor لو عايز تتأكد بسرعة من كل حاجة.

---

## 6. مهمة معلّقة (Pending) — لسه ما اتعملتش

**السياق:** المستخدم رفع ملف `expenses_import_clean.xlsx` وعايز:
1. الشيت يبقى بالكامل بالعربي
2. أسماء الفئات في الشيت تتطابق تمامًا مع أسماء الفئات الموجودة في `expense_categories` (الـ 30 فئة + أي فئة "أخرى" جديدة اتنشئت لاحقًا)
3. الملف يبقى جاهز للرفع مباشرة عبر معالج استيراد المصروفات في النظام (`IMPORT_CONFIGS.expenses` في script.js) بحيث "يبدأ يحسب" فعليًا (يمر فحص الرصيد والفئات بدون أخطاء)

**السبب في التعليق:** بيئة التنفيذ (Bash sandbox) وقعت بالكامل (خطأ mount/RPC من طرف الـ infrastructure نفسها) وفشلت 7 محاولات متتالية بنفس الخطأ. المشكلة مش صلاحيات ولا حاجة أقدر أصلحها من عندي — كانت محتاجة جلسة/محادثة جديدة.

**الملف الأصلي موجود في:** مجلد الرفعات الخاص بالجلسة القديمة (`uploads/expenses_import_clean.xlsx`) — **على الأغلب مش هيكون متاح في الجلسة الجديدة**، فالمستخدم لازم **يرفع الملف تاني** في المحادثة الجديدة.

**خطوات إنهاء المهمة دي في الجلسة الجديدة:**
1. اطلب من المستخدم يرفع `expenses_import_clean.xlsx` تاني لو مش موجود.
2. افتح الملف (Python/openpyxl عبر bash tool) وشوف أعمدته الحالية.
3. طابق أعمدة الملف مع `templateHeaders` بتاعة `IMPORT_CONFIGS.expenses` في script.js (دلوقتي 8 أعمدة):
   `المبلغ | الفئة | التاريخ | الوصف | اسم المورد | رقم الفاتورة | الموظف المستلم للمبلغ (داخلي) | كود العهدة (اختياري)`
4. اسحب قائمة الفئات الفعلية من `database/migration-expense-supplier-invoice-and-categories.sql` (أو اطلب من المستخدم تصدير الفئات من قاعدة البيانات لو فيه فئات جديدة اتضافت بعد كده عبر "أخرى") وطابق كل قيمة في عمود "الفئة" بالشيت مع أقرب فئة عربية مطابقة (`name_ar`).
5. لو فيه قيم في الملف مش مطابقة لأي فئة، إما تسأل المستخدم يحدد الفئة الصح، أو تقترح أقرب فئة موجودة.
6. اكتب الملف النهائي بصيغة `.xlsx` (استخدم مكتبة `openpyxl` أو الأفضل اقرأ `xlsx` skill الأول لو الملف النهائي هيتسلّم كملف Excel).
7. سلّم الملف للمستخدم عبر `present_files` ووضّحله إنه جاهز للرفع من زر "استيراد من Excel" في صفحة المصروفات.

---

## 7. نقاط أمان/تصميم لازم تفتكرها

- عمود `recipient_employee_name` **محمي في الواجهة بس (UI-level)** — الـ RLS الحالي لسياسة `expenses_select` (`is_active_user()`) تقنيًا بيسمح لأي مستخدم نشط يشوفه لو وصل للـ API/devtools مباشرة. اتقال للمستخدم واتعرض عليه تشديد الـ RLS لو عايز حماية فعلية على مستوى قاعدة البيانات — **لسه مطلوبتش**.
- IT Support ممنوع يشوف العهدة/المصروفات في أي مكان — تأكد إن أي فيتشر جديد بيحترم القاعدة دي (خصوصًا في Dashboard وأي كروت جديدة).
- كل الفلوس بالريال السعودي (ر.س).
- التاريخ اليوم وقت كتابة الملف: **13 سبتمبر 2026**.

---

## 8. أسلوب المستخدم (مهم للتفاعل)

- بيتكلم عامية مصرية، بيحب الردود **مختصرة وسريعة** ("بسرعة"، "ظبط").
- بيفضّل ما تسأله أسئلة توضيحية للمهام الكبيرة المعقدة بس، مش لكل حاجة صغيرة.
- بعد أي تعديل كود، لازم تفكّره إنه يعمل **Hard Refresh (Ctrl+Shift+R)** لو التعديل في نص واجهة (cache)، وتفكّره يشغّل أي migration SQL معلّق قبل ما يجرب الفيتشر.
